import React from "react";
import styled from "styled-components";

const Something = () => {
  return <Wrapper>something!</Wrapper>;
};

export default Something;

const Wrapper = styled.div`
  background: tomato;
`;
